<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyldelove.dao.system.SysUserMapper">
  
  <resultMap id="BaseResultMap" type="com.lyldelove.entity.system.SysUser">
    <id column="user_id"  property="userId" />
    <result column="dept_id"  property="deptId" />
    <result column="login_name"  property="loginName" />
    <result column="user_name"  property="userName" />
    <result column="user_type"  property="userType" />
    <result column="password"  property="password" />
    <result column="salt"  property="salt" />
    <result column="email"  property="email" />
    <result column="mobile_phone"  property="mobilePhone" />
    <result column="sex"  property="sex" />
    <result column="avatar"  property="avatar" />
    <result column="status"  property="status" />
    <result column="delete_flag"  property="deleteFlag" />
    <result column="create_by"  property="createBy" />
    <result column="create_time"  property="createTime" />
    <result column="update_by"  property="updateBy" />
    <result column="update_time"  property="updateTime" />
    <result column="remark"  property="remark" />
  </resultMap>

  <sql id="Base_Column_List">
    user_id, dept_id, login_name, user_name, user_type, `password`, salt, email, mobile_phone, 
    sex, avatar, `status`, delete_flag, create_by, create_time, update_by, update_time, 
    remark
  </sql>

  <select id="selectByPrimaryKey" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from sys_user
    where user_id = #{userId}
  </select>

  <delete id="deleteByPrimaryKey">
    delete from sys_user
    where user_id = #{userId}
  </delete>

  <insert id="insert" keyColumn="user_id" keyProperty="userId" useGeneratedKeys="true">
    insert into sys_user (dept_id, login_name, user_name, 
      user_type, `password`, salt, 
      email, mobile_phone, sex, 
      avatar, `status`, delete_flag, 
      create_by, create_time, update_by, 
      update_time, remark)
    values (#{deptId}, #{loginName}, #{userName}, 
      #{userType}, #{password}, #{salt}, 
      #{email}, #{mobilePhone}, #{sex}, 
      #{avatar}, #{status}, #{deleteFlag}, 
      #{createBy}, #{createTime}, #{updateBy}, 
      #{updateTime}, #{remark})
  </insert>

  <update id="updateByPrimaryKey">
    update sys_user
    set dept_id = #{deptId},
      login_name = #{loginName},
      user_name = #{userName},
      user_type = #{userType},
      `password` = #{password},
      salt = #{salt},
      email = #{email},
      mobile_phone = #{mobilePhone},
      sex = #{sex},
      avatar = #{avatar},
      `status` = #{status},
      delete_flag = #{deleteFlag},
      create_by = #{createBy},
      create_time = #{createTime},
      update_by = #{updateBy},
      update_time = #{updateTime},
      remark = #{remark}
    where user_id = #{userId}
  </update>

  <select id="selectUserByLoginName" resultMap="com.lyldelove.entity.system.SysUser">

  </select>

</mapper>